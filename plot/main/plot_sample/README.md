# Introduction
This is a template about reading data with `pandas` and plotting data with `lib.py.plot`.

# Requirements
Install the latest Bazel.

MacOS users can install Bazel with
```bash
brew install bazel
```

# Usage
```bash
bazel run plot
# Draw a CDF figure
bazel run plot -- cdf
```
